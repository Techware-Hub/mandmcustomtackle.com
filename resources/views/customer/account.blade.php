@extends('layouts.app')
@section('content')
<section class="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-bold uppercase tracking-wide text-sky-700">My Account</p><h1 class="mt-2 text-4xl font-black text-blue-950">Welcome, {{ $user->name }}</h1></div><div class="flex gap-3"><a href="{{ route('products.index') }}" class="rounded-lg bg-blue-950 px-5 py-3 font-bold text-white">Continue Shopping</a><form method="POST" action="{{ route('logout') }}">@csrf<button class="rounded-lg border border-sky-200 px-5 py-3 font-bold text-blue-950">Logout</button></form></div></div><div class="mt-8 grid gap-6 lg:grid-cols-[1fr_2fr]"><aside class="rounded-lg border border-sky-100 bg-white p-6 shadow-sm"><h2 class="text-2xl font-bold text-blue-950">Account Information</h2><p class="mt-4">{{ $user->name }}</p><p>{{ $user->email }}</p><div class="mt-6 grid gap-3"><a href="{{ route('customer.orders') }}" class="rounded-lg bg-blue-950 px-4 py-3 text-center font-bold text-white">Order History</a><a href="{{ route('customer.track') }}" class="rounded-lg border border-sky-200 px-4 py-3 text-center font-bold text-blue-950">Track Order</a></div></aside><div class="rounded-lg border border-sky-100 bg-white p-6 shadow-sm"><h2 class="text-2xl font-bold text-blue-950">Recent Orders</h2><div class="mt-5 grid gap-4">@forelse($orders as $order)<a href="{{ route('customer.orders.show', $order) }}" class="rounded-lg border border-sky-100 p-4 hover:bg-sky-50"><strong>{{ $order->order_number }}</strong><span class="ml-3 text-sm capitalize text-slate-600">{{ $order->order_status }}</span><span class="float-right font-bold">${{ number_format((float)$order->total_amount, 2) }}</span></a>@empty<p class="rounded-lg bg-sky-50 p-4 text-slate-700">No orders yet.</p>@endforelse</div></div></div></section>
@endsection
