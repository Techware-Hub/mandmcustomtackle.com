@extends('layouts.app')
@section('content')
<section class="mx-auto max-w-3xl px-4 py-14 sm:px-6 lg:px-8"><div class="rounded-lg border border-sky-100 bg-white p-6 shadow-sm"><h1 class="text-3xl font-black text-blue-950">Track Order</h1><form method="POST" action="{{ route('customer.track.search') }}" class="mt-6 grid gap-4">@csrf<label>Order Number<input name="order_number" value="{{ old('order_number') }}" class="mt-2 w-full rounded-lg border border-sky-200 px-3 py-3" required></label><label>Email<input name="email" type="email" value="{{ old('email') }}" class="mt-2 w-full rounded-lg border border-sky-200 px-3 py-3" required></label><button class="rounded-lg bg-blue-950 px-5 py-3 font-bold text-white">Track Order</button></form>@isset($searched)<div class="mt-6 rounded-lg bg-sky-50 p-4">@if($order)<h2 class="font-black text-blue-950">{{ $order->order_number }}</h2><p class="mt-2 capitalize">Order status: {{ $order->order_status }}</p><p class="capitalize">Payment status: {{ $order->payment_status }}</p><p>Total: ${{ number_format((float)$order->total_amount, 2) }}</p>@else<p>No order matched that order number and email.</p>@endif</div>@endisset</div></section>
@endsection
