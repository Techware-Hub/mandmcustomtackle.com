@extends('layouts.app')
@section('content')
<section class="mx-auto max-w-6xl px-4 py-14 sm:px-6 lg:px-8"><h1 class="text-4xl font-black text-blue-950">Order {{ $order->order_number }}</h1><div class="mt-8 grid gap-6 lg:grid-cols-[1fr_360px]"><div class="rounded-lg border border-sky-100 bg-white p-6 shadow-sm"><h2 class="text-2xl font-bold text-blue-950">Items</h2>@foreach($order->items as $item)<div class="mt-4 flex justify-between gap-4 border-t border-sky-100 pt-4"><div class="flex gap-3"><x-product-image :product="$item->product" :name="$item->product_name" variant="summary" /><span>{{ $item->product_name }} x {{ $item->quantity }} @if($item->variant_color || $item->variant_weight)<span class="block text-sm text-slate-600">{{ $item->variant_color }} / {{ $item->variant_weight }}</span>@endif</span></div><strong>${{ number_format((float)$item->total, 2) }}</strong></div>@endforeach</div><aside class="rounded-lg border border-sky-100 bg-white p-6 shadow-sm"><h2 class="text-2xl font-bold text-blue-950">Tracking</h2><p class="mt-4 capitalize">Order: {{ $order->order_status }}</p><p class="capitalize">Payment: {{ $order->payment_status }}</p><p class="mt-4 font-black text-sky-700">${{ number_format((float)$order->total_amount, 2) }}</p></aside></div></section>
@endsection
