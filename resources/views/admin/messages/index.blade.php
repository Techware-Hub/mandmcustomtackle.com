@extends('admin.layouts.app')
@section('page-title','Contact Messages')
@section('content')
<div class="mb-5 flex gap-2"><a href="{{ route('admin.messages.index') }}" class="rounded-lg border border-slate-700 px-3 py-2">All Messages</a><a href="{{ route('admin.messages.index', ['status' => 'unread']) }}" class="rounded-lg border border-slate-700 px-3 py-2">Unread Messages</a></div>
<div class="admin-card overflow-hidden rounded-lg border border-slate-800 bg-slate-900"><table class="w-full text-left text-sm"><thead class="bg-slate-950 text-slate-400"><tr><th class="p-3">Name</th><th class="p-3">Email</th><th class="p-3">Subject</th><th class="p-3">Date</th><th class="p-3">Status</th><th class="p-3">Actions</th></tr></thead><tbody>@forelse($messages as $message)<tr class="border-t border-slate-800"><td class="p-3 font-bold text-white">{{ $message->name }}</td><td class="p-3">{{ $message->email }}</td><td class="p-3">{{ $message->subject }}</td><td class="p-3">{{ $message->created_at->format('M j, Y') }}</td><td class="p-3">{{ $message->read_at ? 'Read' : 'Unread' }}</td><td class="p-3 flex gap-3"><a href="{{ route('admin.messages.show', $message) }}" class="font-bold text-sky-300">View</a><form method="POST" action="{{ route('admin.messages.destroy', $message) }}">@csrf @method('DELETE')<button class="font-bold text-red-300">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="p-5">No messages.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $messages->links() }}</div>
@endsection
