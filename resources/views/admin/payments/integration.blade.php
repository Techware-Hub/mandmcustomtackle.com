@extends('admin.layouts.app')
@section('page-title','Payment Integration')
@section('content')
<div class="grid gap-6 xl:grid-cols-2">
    <div class="admin-card rounded-lg border border-slate-800 bg-slate-900 p-6"><h2 class="text-xl font-black text-white">Current Payment Methods</h2><ul class="mt-5 grid gap-3 text-slate-300"><li>Cash on Delivery / Pay Later</li><li>Manual Payment</li><li>Card Payment Placeholder</li></ul></div>
    <form method="POST" action="{{ route('admin.payments.integration.update') }}" class="admin-card rounded-lg border border-slate-800 bg-slate-900 p-6">@csrf @method('PUT')<h2 class="text-xl font-black text-white">Integration Settings</h2><p class="mt-2 text-sm text-slate-400">TODO: Wire these settings to real Stripe/PayPal SDK flows when live credentials are available.</p><label class="mt-4 block">Stripe Public Key<input name="stripe_public_key" value="{{ $settings['stripe_public_key'] ?? '' }}" class="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2"></label><label class="mt-4 block">Stripe Secret Key<input name="stripe_secret_key" value="{{ $settings['stripe_secret_key'] ?? '' }}" class="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2"></label><label class="mt-4 block">PayPal Client ID<input name="paypal_client_id" value="{{ $settings['paypal_client_id'] ?? '' }}" class="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2"></label><label class="mt-4 block">PayPal Secret<input name="paypal_secret" value="{{ $settings['paypal_secret'] ?? '' }}" class="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2"></label><label class="mt-4 flex gap-2"><input type="checkbox" name="enable_stripe" value="1" @checked(($settings['enable_stripe'] ?? '0') === '1')> Enable Stripe</label><label class="mt-2 flex gap-2"><input type="checkbox" name="enable_paypal" value="1" @checked(($settings['enable_paypal'] ?? '0') === '1')> Enable PayPal</label><button class="mt-5 rounded-lg bg-sky-500 px-5 py-2 font-bold text-slate-950">Save Payment Settings</button></form>
</div>
@endsection
