@extends('admin.layouts.app')
@section('page-title', 'All Orders')
@section('content')
<div class="mb-5 flex gap-2"><a href="{{ route('admin.orders.index') }}" class="rounded-lg border border-slate-700 px-3 py-2">All</a>@foreach(['pending','processing','completed','cancelled'] as $status)<a href="{{ route('admin.orders.index', ['status' => $status]) }}" class="rounded-lg border border-slate-700 px-3 py-2 capitalize">{{ $status }}</a>@endforeach</div>
<div class="admin-card overflow-hidden rounded-lg border border-slate-800 bg-slate-900"><table class="w-full text-left text-sm"><thead class="bg-slate-950 text-slate-400"><tr><th class="p-3">Order</th><th class="p-3">Customer</th><th class="p-3">Email</th><th class="p-3">Phone</th><th class="p-3">Total</th><th class="p-3">Payment</th><th class="p-3">Status</th><th class="p-3">Created</th><th class="p-3">View</th></tr></thead><tbody>@forelse($orders as $order)<tr class="border-t border-slate-800"><td class="p-3 font-bold text-white">{{ $order->order_number }}</td><td class="p-3">{{ $order->customer_name }}</td><td class="p-3">{{ $order->customer_email }}</td><td class="p-3">{{ $order->customer_phone }}</td><td class="p-3">${{ number_format((float)$order->total_amount, 2) }}</td><td class="p-3 capitalize">{{ $order->payment_status }}</td><td class="p-3 capitalize">{{ $order->order_status }}</td><td class="p-3">{{ $order->created_at->format('M j, Y') }}</td><td class="p-3"><a href="{{ route('admin.orders.show', $order) }}" class="font-bold text-sky-300">View</a></td></tr>@empty<tr><td colspan="9" class="p-5">No orders.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $orders->links() }}</div>
@endsection
